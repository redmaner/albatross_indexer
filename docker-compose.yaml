# Use root/example as user/password credentials
version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: albatross
      MONGO_INITDB_ROOT_PASSWORD: "a safe password here"
      MONGO_INITDB_DATABASE: albatross
    ports:
    - 27017:27017

  # mongo-express:
  #   image: mongo-express
  #   restart: always
  #   ports:
  #     - 8081:8081
  #   environment:
  #     ME_CONFIG_MONGODB_ADMINUSERNAME: albatross
  #     ME_CONFIG_MONGODB_ADMINPASSWORD: "a safe password here"
  #     ME_CONFIG_MONGODB_URL: "mongodb://albatross:a safe pasword here@mongo:27017/"